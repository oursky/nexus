# Xcode derived data and user settings
*.xcuserstate
xcuserdata/
DerivedData/
*.xcworkspace/xcuserdata/

# SPM build artifacts
.build/

# macOS nexus binary — no longer bundled (app-sandbox crash)
# Regenerated by scripts/swift/stage-macos-nexus.sh
# Linux binaries (nexus-linux-*) are still needed for provisioning
Resources/nexus
