// Package hostsetup detects the host OS package manager and installs
// guest-rootfs tools (e2fsprogs, coreutils, zstd) and VM runtime
// dependencies (libkrun, passt) needed by the Nexus daemon.
package hostsetup
