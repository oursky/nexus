# Design System

This document previously described the design system for `packages/nexus-swift` (Nexus macOS Swift app). That package is not part of this repository.

The macOS Swift app and its design system are maintained separately.
