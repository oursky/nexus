version: "3"

dotenv: [".env.local"]

vars:
  REMOTE_HOST: '{{.REMOTE_HOST | default ""}}'
  REMOTE_BIN:  '{{.REMOTE_BIN  | default "~/.local/bin/nexus"}}'
  REMOTE_PORT: '{{.REMOTE_PORT | default "7777"}}'

tasks:
  # ── Setup ────────────────────────────────────────────────────────────────
  setup:
    desc: "Check prerequisites and install Go dependencies"
    cmds:
      - |
        fail=0

        if ! command -v go >/dev/null 2>&1; then
          echo "✗ go not found (need ≥1.21)" && fail=1
        else
          go_ver=$(go version | grep -oE '[0-9]+\.[0-9]+' | head -1)
          go_major=$(echo "$go_ver" | cut -d. -f1)
          go_minor=$(echo "$go_ver" | cut -d. -f2)
          if [ "$go_major" -lt 1 ] || { [ "$go_major" -eq 1 ] && [ "$go_minor" -lt 21 ]; }; then
            echo "✗ go $go_ver found but ≥1.21 required" && fail=1
          else
            echo "✓ go $go_ver"
          fi
        fi

        if ! command -v task >/dev/null 2>&1; then
          echo "✗ task not found — install: https://taskfile.dev/installation" && fail=1
        else
          echo "✓ task $(task --version 2>/dev/null | head -1)"
        fi

        [ "$fail" -eq 0 ] || { echo ""; echo "Fix the issues above then re-run: task setup"; exit 1; }
      - cd packages/nexus && go mod download
      - 'echo "Setup complete. Next: task dev:remote"'

  # ── Checks ───────────────────────────────────────────────────────────────
  build:
    desc: "Compile all Go packages"
    dir: packages/nexus
    cmds:
      - go build ./...

  test:
    desc: "Run Go tests"
    dir: packages/nexus
    cmds:
      - go test ./...

  ci:
    desc: "Run local CI equivalent (go-fix + coverage + core)"
    cmds:
      - ./scripts/ci/go-fix.sh
      - ./scripts/ci/coverage.sh
      - ./scripts/ci/nexus-core.sh

  ci:libkrun:prep:
    desc: "Prepare libkrun VM assets for CI/e2e (host prereqs, build nexus, provision, prewarm)"
    cmds:
      - ./scripts/ci/install-host-prereqs.sh
      - ./scripts/ci/build-nexus-libkrun.sh
      - ./scripts/ci/provision-libkrun-host.sh
      - ./scripts/ci/prewarm-default-profile.sh

  ci:libkrun:prebuilt:
    desc: "Build and package prebuilt libkrun VM assets"
    cmds:
      - ./scripts/ci/install-host-prereqs.sh
      - ./scripts/ci/build-nexus-libkrun.sh
      - ./scripts/ci/build-prebuilt-vm-assets.sh
      - ./scripts/ci/package-prebuilt-vm-assets.sh

  clean:
    desc: "Remove build artifacts"
    cmds:
      - rm -rf packages/nexus/nexus packages/nexus/tmp packages/nexus/.air-tmp

  # ── Dev loops ─────────────────────────────────────────────────────────────
  # Set REMOTE_HOST=user@hostname in .env.local (see .env.local.example).

  dev:remote:
    desc: "Build for linux/amd64, deploy, restart remote daemon, and stage Resources so provision stays in sync"
    preconditions:
      - sh: '[ -n "{{.REMOTE_HOST}}" ]'
        msg: "REMOTE_HOST is not set. Add it to .env.local or pass REMOTE_HOST=user@host on the CLI."
    env:
      REMOTE_HOST: "{{.REMOTE_HOST}}"
      REMOTE_BIN:  "{{.REMOTE_BIN}}"
    cmds:
      - scripts/remote/deploy.sh       # builds linux binary, stages to Resources/, deploys to remote
      - scripts/remote/daemon-restart.sh  # restarts daemon + prints remote version for confirmation

  dev:cli:
    desc: "install local CLI binary (for CLI-only changes)"
    preconditions:
      - sh: '[ -n "{{.REMOTE_HOST}}" ]'
        msg: "REMOTE_HOST is not set. Add it to .env.local or pass REMOTE_HOST=user@host on the CLI."
    env:
      REMOTE_HOST: "{{.REMOTE_HOST}}"
      REMOTE_BIN:  "{{.REMOTE_BIN}}"
    cmds:
      - scripts/local/deploy.sh

  dev:libkrun:
    desc: "Build libkrun-enabled nexus (downloads libs, embeds nexus-libkrun-vm) and deploy to remote"
    preconditions:
      - sh: '[ -n "{{.REMOTE_HOST}}" ]'
        msg: "REMOTE_HOST is not set. Add it to .env.local or pass REMOTE_HOST=user@host on the CLI."
    env:
      REMOTE_HOST: "{{.REMOTE_HOST}}"
      REMOTE_BIN:  "{{.REMOTE_BIN}}"
    cmds:
      - scripts/remote/deploy-libkrun.sh

  dev:libkrun:rebuild-vm:
    desc: "Rebuild nexus-libkrun-vm on remote Linux host when local source is newer (macOS contributors)"
    preconditions:
      - sh: '[ -n "{{.REMOTE_HOST}}" ]'
        msg: "REMOTE_HOST is not set. Add it to .env.local or pass REMOTE_HOST=user@host on the CLI."
    env:
      REMOTE_HOST: "{{.REMOTE_HOST}}"
    cmds:
      - scripts/local/rebuild-libkrun-vm.sh

  dev:swift:
    desc: "dev:cli + stage macOS nexus binary + regenerate Swift SDK + build and open NexusApp"
    preconditions:
      - sh: '[ -n "{{.REMOTE_HOST}}" ]'
        msg: "REMOTE_HOST is not set. Add it to .env.local or pass REMOTE_HOST=user@host on the CLI."
    env:
      REMOTE_HOST: "{{.REMOTE_HOST}}"
      REMOTE_BIN:  "{{.REMOTE_BIN}}"
    cmds:
      - task: dev:cli
      - task: stage:nexus-macos
      - task: stage:nexus-linux
      - scripts/generate-sdk.sh
      - scripts/swift/build.sh
      - scripts/swift/open.sh

  stage:nexus-macos:
    desc: "Build macOS arm64 nexus CLI and stage to packages/nexus-swift/Resources/nexus"
    cmds:
      - |
        BUILD_TIME="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        GIT_COMMIT="$(git rev-parse --short HEAD 2>/dev/null || echo dev)"
        LDFLAGS="-X github.com/oursky/nexus/packages/nexus/internal/buildinfo.Time=${BUILD_TIME} \
                 -X github.com/oursky/nexus/packages/nexus/internal/buildinfo.Commit=${GIT_COMMIT}"
        echo "Staging nexus (darwin/arm64) → packages/nexus-swift/Resources/nexus (commit=${GIT_COMMIT} built=${BUILD_TIME})..."
        GOOS=darwin GOARCH=arm64 go build \
          -C packages/nexus \
          -ldflags "$LDFLAGS" \
          -o ../../packages/nexus-swift/Resources/nexus \
          ./cmd/nexus
        chmod +x packages/nexus-swift/Resources/nexus
        echo "✓ Staged $(ls -lh packages/nexus-swift/Resources/nexus | awk '{print $5}')"

  stage:nexus-linux:
    desc: "Build linux/amd64 nexus daemon and stage to packages/nexus-swift/Resources/nexus-linux-amd64 (bundled for remote provisioning)"
    cmds:
      - scripts/swift/stage-linux-nexus.sh amd64

  # ── Maintenance ───────────────────────────────────────────────────────────
  generate:sdk:
    desc: "Regenerate NexusRPC.swift from Go RPC types"
    cmds:
      - scripts/generate-sdk.sh

  showcase:rerecord:
    desc: "Re-record showcase flow by controlling NexusApp through headless RPC, then render remotion"
    cmds:
      - pnpm --dir packages/showcase run rerecord
